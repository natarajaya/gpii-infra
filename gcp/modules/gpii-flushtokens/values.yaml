image:
  repository: ${flushtokens_repository}
  checksum: ${flushtokens_checksum}

couchdb:
  url: "http://${couchdb_admin_username}:${couchdb_admin_password}@${couchdb_active_cluster_name}-svc-couchdb.gpii.svc.cluster.local:5984/gpii"

couchdbClusterName: ${couchdb_active_cluster_name}
